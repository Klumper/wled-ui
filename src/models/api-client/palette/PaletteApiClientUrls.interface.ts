/**
 * @Name PaletteApiClientUrlsInterface
 * @description
 * Interface for the palette urls used to avoid hard-coded strings
 */
export interface PaletteApiClientUrlsInterface {
  fetchPalette: string;
}