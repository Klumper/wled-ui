import { StateInterface } from '@/models/state';

/**
 * @name StateStateInterface
 * @description
 * Interface for the state state
 */
export interface StateStateInterface {
  loading: boolean;
  state: StateInterface;
}