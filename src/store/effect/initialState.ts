import { EffectStateInterface } from '@/models/store';

/**
 * @name initialEffectState
 * @description
 * The Effect state instance with the initial default values
 */
export const initialEffectState: EffectStateInterface = {
  loading: false,
  effects: [],
};