import { PaletteStateInterface } from '@/models/store';

/**
 * @name initialPaletteState
 * @description
 * The Items state instance with the initial default values
 */
export const initialPaletteState: PaletteStateInterface = {
  loading: false,
  palettes: [],
};